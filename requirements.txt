########################################
# Production Dependencies
# (Pinned for reproducibility; update thoughtfully to avoid breaking runtime behavior)
########################################
Flask==3.0.3
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.30        # Explicit pin (transitive via Flask-SQLAlchemy) for consistent core/ORM behavior
Flask-WTF==1.2.1
email-validator==2.2.0
itsdangerous==2.2.0
pandas==2.2.2              # Used for attendance spreadsheet ingestion
openpyxl==3.1.2            # XLSX reading
xlrd==2.0.1                # Legacy XLS support (keep while old formats still appear)
requests==2.32.3
xhtml2pdf==0.2.15          # HTML to PDF for reports/invoices
APScheduler==3.10.4        # Background scheduling (appointment reminders)

########################################
# Development / Testing
########################################
pytest==8.2.2

########################################
# Upgrade Guidance
# - Bump versions cautiously; run full test suite & manual smoke of:
#   * Observations extended report (PDF/email rendering)
#   * Appointment reminder scheduling (APScheduler init)
#   * Invoice print & email flow (xhtml2pdf / HTML generation)
# - When updating SQLAlchemy / Flask-SQLAlchemy ensure no deprecation warnings for Session/Query patterns.
########################################
